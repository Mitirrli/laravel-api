version: "3.7"

services:
    laravel-api:
        container_name: laravel-api
        build:
            context: .docker/php
            dockerfile: Dockerfile
        ports:
            - 8088:8000
        volumes:
            - ./:/www
        tty: true
        restart: always
