extend type Query {
    "获取产品详情"
    product (id: ID! @eq): Products @find
}

type Products {
    id: ID!
    uid: Int!
    product_name: String!
    manufacturer_name: String!
    pictures: String!
    specification: String!
    approval_number: String!
    advantage: String!
    video: String!
}

